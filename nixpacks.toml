[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "streamlit run stock_predictor.py --server.port $PORT --server.address 0.0.0.0"
